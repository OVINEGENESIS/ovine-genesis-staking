const connectBtn = document.getElementById('connectBtn'); const walletChip = document.getElementById('walletChip'); const demoNote = document.getElementById('demoNote'); const CHAIN_ID = 4663; const CHAIN_ID_HEX = '0x1237'; const ROBINHOOD_CHAIN = { chainId: CHAIN_ID_HEX, chainName: 'Robinhood Chain', nativeCurrency: { name: 'Ether', symbol: 'ETH', decimals: 18 }, rpcUrls: ['https://rpc.mainnet.chain.robinhood.com'], blockExplorerUrls: ['https://robinhoodchain.blockscout.com'] }; let account = null; function shortenAddress(address) { return `${address.slice(0, 6)}...${address.slice(-4)}`; } function setConnected(address) { account = address; const short = shortenAddress(address); connectBtn.textContent = short; walletChip.textContent = `Connected: ${short}`; demoNote.textContent = 'Wallet connected. NFT ownership check will be added next.'; } function setDisconnected() { account = null; connectBtn.textContent = 'Connect Wallet'; walletChip.textContent = 'Wallet not connected'; demoNote.textContent = 'Connect your wallet to continue.'; } async function switchToRobinhood() { if (!window.ethereum) { throw new Error('No EVM wallet detected.'); } try { await window.ethereum.request({ method: 'wallet_switchEthereumChain', params: [{ chainId: CHAIN_ID_HEX }] }); } catch (error) { // Error 4902 = chain not added to wallet if (error.code === 4902) { await window.ethereum.request({ method: 'wallet_addEthereumChain', params: [ROBINHOOD_CHAIN] }); } else { throw error; } } } async function connectWallet() { if (!window.ethereum) { alert( 'No compatible wallet detected. Please install MetaMask or another EVM-compatible wallet.' ); return; } try { const accounts = await window.ethereum.request({ method: 'eth_requestAccounts' }); if (!accounts || accounts.length === 0) { return; } await switchToRobinhood(); const chainId = await window.ethereum.request({ method: 'eth_chainId' }); if (parseInt(chainId, 16) !== CHAIN_ID) { alert('Please switch to Robinhood Chain.'); return; } setConnected(accounts[0]); } catch (error) { console.error(error); alert( error?.message || 'Wallet connection failed.' ); } } async function handleAccountsChanged(accounts) { if (!accounts || accounts.length === 0) { setDisconnected(); return; } setConnected(accounts[0]); } async function handleChainChanged(chainId) { if (parseInt(chainId, 16) !== CHAIN_ID) { walletChip.textContent = 'Wrong network'; demoNote.textContent = 'Please switch to Robinhood Chain to use Ovine Genesis staking.'; return; } if (account) { setConnected(account); } } connectBtn.addEventListener('click', connectWallet); if (window.ethereum) { window.ethereum.on('accountsChanged', handleAccountsChanged); window.ethereum.on('chainChanged', handleChainChanged); } walletChip.textContent = 'Wallet not connected';
